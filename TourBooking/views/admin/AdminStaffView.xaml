<UserControl x:Class="TourBooking.Views.Admin.AdminStaffView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1200"
             Background="#F4F6F9"
             FontFamily="Segoe UI">

    <UserControl.Resources>
        <Style TargetType="Border" x:Key="CardBorder">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="15" ShadowDepth="0" Color="#E2E8F0" Opacity="0.7"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Tạo Style chung để căn giữa dọc tất cả các chữ trong ô DataGridTextColumn -->
        <Style x:Key="CenterTextColumnStyle" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- TẦNG 1: TOP HEADER -->
        <Grid Grid.Row="0" Margin="10,0,10,15">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Quản lý nhân viên" FontSize="22" FontWeight="Bold" Foreground="#0A1942"/>
                <TextBlock Text="Tổng quan danh sách nhân sự vận hành hệ thống" FontSize="13" Foreground="#667085" Margin="0,4,0,0"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" Width="280" Height="36" VerticalContentAlignment="Center" Padding="10,0" BorderBrush="#D0D5DD" Background="White">
                    <TextBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>
            </StackPanel>
        </Grid>

        <!-- TẦNG 2: KPI TILES -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Border Background="White" CornerRadius="6" BorderBrush="#EAECF0" BorderThickness="1" Width="180" Padding="15,12" Margin="10,0,10,0">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Tổng nhân viên" Foreground="#667085" FontSize="11" FontWeight="Bold"/>
                        <TextBlock Text="{Binding TotalStaffCount}" FontSize="24" FontWeight="Bold" Foreground="#1D3D8F" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
                <Border Background="White" CornerRadius="6" BorderBrush="#EAECF0" BorderThickness="1" Width="180" Padding="15,12" Margin="0,0,10,0">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Đang hoạt động" Foreground="#667085" FontSize="11" FontWeight="Bold"/>
                        <TextBlock Text="{Binding ActiveStaffCount}" FontSize="24" FontWeight="Bold" Foreground="#12B76A" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
            </StackPanel>
            <Button Content="+ Thêm nhân viên" Background="#0A1942" Foreground="White" FontWeight="Bold" HorizontalAlignment="Right" Padding="22,12" Margin="0,0,10,0" VerticalAlignment="Center" Cursor="Hand" Command="{Binding AddStaffCommand}">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>

        <!-- TẦNG 3: DATA GRID CHÍNH -->
        <Border Grid.Row="2" Style="{StaticResource CardBorder}" Padding="15">
            <DataGrid ItemsSource="{Binding FilteredStaffs}" Background="Transparent" BorderThickness="0" AutoGenerateColumns="False" 
                      CanUserAddRows="False" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#EAECF0" HeadersVisibility="Column" RowHeight="60">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F2F4F7"/>
                        <Setter Property="Foreground" Value="#475467"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="12"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.Columns>
                    <!-- Đã sửa: Dùng ElementStyle thay cho thuộc tính gõ trực tiếp -->
                    <DataGridTextColumn Header="MÃ NV" Binding="{Binding StaffCode}" Width="1.2*" FontWeight="SemiBold" Foreground="#475467" ElementStyle="{StaticResource CenterTextColumnStyle}"/>

                    <DataGridTemplateColumn Header="HỌ TÊN" Width="2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Border Width="30" Height="30" CornerRadius="15" Background="#EAECF0" Margin="0,0,10,0">
                                        <TextBlock Text="👤" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="12"/>
                                    </Border>
                                    <TextBlock Text="{Binding FullName}" FontWeight="Bold" Foreground="#101828" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Đã sửa: Áp dụng ElementStyle -->
                    <DataGridTextColumn Header="EMAIL / ACCOUNT" Binding="{Binding Username}" Width="2.5*" ElementStyle="{StaticResource CenterTextColumnStyle}"/>
                    <DataGridTextColumn Header="VAI TRÒ HỆ THỐNG" Binding="{Binding Role}" Width="1.5*" ElementStyle="{StaticResource CenterTextColumnStyle}"/>

                    <DataGridTemplateColumn Header="TRẠNG THÁI" Width="1.3*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="12" Padding="10,5" HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#D1FADF"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                    <Setter Property="Background" Value="#FECDCA"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock FontSize="11" FontWeight="Bold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="Hoạt động"/>
                                                <Setter Property="Foreground" Value="#12B76A"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                        <Setter Property="Text" Value="Đang khóa"/>
                                                        <Setter Property="Foreground" Value="#F04438"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="THAO TÁC" Width="1.2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Button Content="Sửa" Background="Transparent" Foreground="#1D3D8F" BorderThickness="0" FontWeight="SemiBold" Margin="0,0,12,0" Cursor="Hand" Command="{Binding DataContext.EditStaffCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                    <Button Background="Transparent" BorderThickness="0" FontWeight="SemiBold" Cursor="Hand" Command="{Binding DataContext.ToggleStatusCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Content" Value="Khóa"/>
                                                <Setter Property="Foreground" Value="#F04438"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                        <Setter Property="Content" Value="Mở"/>
                                                        <Setter Property="Foreground" Value="#12B76A"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- TẦNG 4: BOTTOM BANNER -->
        <UniformGrid Grid.Row="3" Columns="3" Margin="0,10,0,0">
            <Border Background="#EFF4FF" CornerRadius="6" Padding="15,12" Margin="10,5">
                <StackPanel>
                    <TextBlock Text="BẢO MẬT HỆ THỐNG" Foreground="#1D3D8F" FontSize="12" FontWeight="Bold"/>
                    <TextBlock Text="Yêu cầu nhân viên thay đổi mật khẩu định kỳ để đảm bảo an toàn dữ liệu." Foreground="#475467" FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
            <Border Background="#ECFDF3" CornerRadius="6" Padding="15,12" Margin="10,5">
                <StackPanel>
                    <TextBlock Text="XỬ LÝ NHANH" Foreground="#12B76A" FontSize="12" FontWeight="Bold"/>
                    <TextBlock Text="Sử dụng tính năng 'Khóa' ngay lập tức khi phát hiện truy cập bất thường từ tài khoản." Foreground="#475467" FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
            <Border Background="#F9F5F0" CornerRadius="6" Padding="15,12" Margin="10,5">
                <StackPanel>
                    <TextBlock Text="PHÂN QUYỀN" Foreground="#B45309" FontSize="12" FontWeight="Bold"/>
                    <TextBlock Text="Hệ thống phân quyền theo module giúp tối ưu hóa luồng công việc cho từng bộ phận điều hành." Foreground="#475467" FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
        </UniformGrid>
    </Grid>
</UserControl>